/**
 * S2 (backend) and S3 (chatbot) path contracts live here once those teams publish them.
 * Do not invent endpoint paths in the frontend.
 */
export const S2_ENDPOINTS = {} as const

export const S3_ENDPOINTS = {} as const
